import { describe, expect, it } from 'vitest';

it.todo('Following tests are broken due to changed import from quasar');

// import fullRoutes, { navRoutes } from '@/router/routes';
//
// describe('Routes', () => {
//     it('should have correct value: fullRoutes', async () => {
//         expect(fullRoutes).to.exist.and.to.be.an('array');
//         expect(fullRoutes).to.have.nested.property('[0].path', '/');
//     });
//
//     it.todo('should have correct behaviour: dynamic import');
//
//     it('should have correct value: navRoutes', async () => {
//         expect(navRoutes).to.exist.and.to.be.an('array');
//         expect(navRoutes).to.have.lengthOf(fullRoutes.length);
//         expect(navRoutes).to.have.nested.property('[0].to', '/');
//     });
// });
